# 05 · 演示与答疑

> **上台当天带这一份 + `演讲稿.md`。**
> 演示步骤、兜底方案、Q&A 应答。**不含录屏方案**（不做视频）。

---

## 一、演示方案

### 1.1 演示要证明的三件事

| # | 要证明 | 观众看到什么 |
|---|---|---|
| 1 | 它能**只读地**分析并给出计划 | 分析师不动代码，只出计划 |
| 2 | ⭐ 它**会停下来等人** | 计划产出后**停住**，等你说可以 |
| 3 | 点头之后它**真能改并验证** | 改动 + 测试通过 |

> **第 2 件比第 3 件重要。**
> 如果只能成功一半，宁可"分析得好、停得漂亮、最后没改完"，也不要"改得飞快、从头到尾没停过"。
> **因为你讲的是可控性，不是速度。**

### 1.2 演示数据

用 **CSV 兜底作为主线**，不接 ADO。理由：不依赖内网权限、不依赖网络、不依赖别人没动过那条工单。

数据文件：`demo/issues-sample.csv`（3 条：2 bug + 1 feature，10 列，UTF-8 BOM + CRLF，Excel 双击不乱码）。

> **用 ADO 真数据的情况**：想增加"这是真的"的说服力，就提前**锁定一条你自己提的、确定没人会动的**工单并截图备份。
> ⚠️ **不要现场去 ADO 里翻找**——那是演示翻车的头号原因。

### 1.3 演示步骤（3–5 分钟）

| 步 | 你做什么 | 你旁边说什么 | 时长 |
|---|---|---|---|
| 0 | 展示那条 bug | "这是财务提的一个 bug，导出 CSV 列名乱码。" | 20s |
| 1 | 让分析师接手 | "注意——**它是只读的**。" | — |
| 2 | 分析师评估 + 只读定位 | "它在读代码，但它**动不了**任何文件。" | 60–90s |
| 3 | ⭐ **它出计划，然后停下** | ⭐ **"注意——它现在停下来了。它在等我说可以。"** | **停 5 秒** |
| 4 | 你点头 | "我点了头，它才开始改。" | 10s |
| 5 | 实现者动手 | "它现在能写文件了，但**推不出去**。" | 60–90s |
| 6 | 验收者独立复查 + 跑测试 | "换了一个全新的上下文，而且换了**另一个模型**来看这段改动。" | 30–60s |
| 7 | 看留痕 | "每一次审批请求、每一个决定，都在日志里。" | 20s |

**⭐ 第 3 步的那 5 秒沉默是整场的高潮。**
很多人会忍不住在这 5 秒里继续说——**别说。** 让它安静地停在那里，让观众自己看见。

### 1.4 最小可行版本（90 秒）

时间紧或准备不足时，**只演第 1–4 步**（分析 → 计划 → 停 → 点头），实现和验收口述带过。

> 这个版本 90 秒演完，而且**保住了最重要的那一秒**。

### 1.5 T-1 天准备清单

- [ ] 演示完整跑通**至少 2 遍**
- [ ] **把关键画面截成静态图**（兜底用，见第二节）
- [ ] 演示的 bug 选定：**真、但小**，改动一屏看得完
- [ ] `demo/issues-sample.csv` 能正确解析（`Import-Csv` → 3 行 × 10 列）
- [ ] `dsh` 可用、Key 有效
- [ ] 若演示 OpenDesign 接入：**确认 OD daemon 正在跑**（否则 `od agent setup` 会 exit 64）
- [ ] 终端**字号调大**，关掉所有通知

---

## 二、兜底方案（静态截图，不录屏）

### 2.1 截图要截哪几张

**按演示的 8 步各截一张**，按顺序放在一个文件夹里，文件名编号：

```
demo/shots/
├── 01-投单.png
├── 02-只读定位.png
├── 03-⭐停下等批准.png      ← 最重要
├── 04-点头后动手.png
├── 05-验收独立复查.png
└── 06-审计留痕.png
```

> **第 3 张是命根子。** 就算别的都没截，这一张必须有。

### 2.2 什么时候切换到截图

**3 秒内切。** 触发条件：
- 卡住 / 转圈**超过 3 秒**
- 报错
- 输出的东西明显不对
- 网络 / API 异常

切换话术（**原话照说**）：

> "我先把后面几步的样子放出来，这样不耽误大家时间。"

然后**按编号往后翻**，照常讲。

### 2.3 不要说

| ❌ | ✅ |
|---|---|
| "奇怪，刚才还好好的" | 直接切，别评价 |
| "稍等我看看" | 不要现场 debug |
| "可能是网络问题" | 不提"网络"两个字 |

> **在台上 debug 才是丢分。** 提前截图恰恰说明你准备过。

### 2.4 如果它做的事和你讲的不一样

**承认，并指回主线：**

> "它自己多做了一步我没让它做的。
> 这恰好是我为什么要强调留痕——**不然你根本不知道它干了什么**。"

⚠️ 不要说"这就是 AI 的不确定性，很有趣"——那会让你讲的东西显得不可靠。**把意外变成论据。**

### 2.5 有人打断："这是不是演好的？"

**欢迎他：**

> "非常欢迎质疑。要不你上来点一下？我把键盘给你。"

**主动邀请比辩解有效一百倍。**

---

## 三、Q&A 应答

### 3.1 敏感问题（一定会被问）

#### ❓ "这会不会取代我们？会不会裁员？"

> "我今天的例子里，整条链路唯一**不可替代**的一步，是'人点头'。
> 能自动化掉的都是**劳动**，不能自动化的是**判断**。
>
> 但我不打算骗你们说这不会改变岗位内容。**它会。**
> 它会把'搬砖'那部分挤掉，把'要不要做、做到什么程度、能不能合'这部分放大。
>
> 所以真正的问题不是'AI 会不会取代我'，
> 是**你在流程里站的是搬砖那个位置，还是点头那个位置。**"

📋 要点：承认影响 + 不承诺 + 把问题转成"你站哪个位置"。

---

#### ❓ "安全吗？让 AI 碰我们的代码库"

> "我今天讲的所有设计，都在回答这一个问题。四个层次——
> **一、只读优先**：三个岗位里两个完全只读，唯一能写的那一个写了也推不出去。
> **二、权限是运行时强制的**：能在提示词里写'请不要'，就说明这个边界是软的。真正的边界是沙箱和审批 seam。
> **三、唯一合入权在人手上。**
> **四、全程留痕。**
>
> 一句话：**我没让它更自由，我让它更受约束。**"

---

#### ❓ "要花多少钱？"

**不给数字。**

> "分两块：模型调用和工具。
> 工具这块 DSH 和 OpenDesign 都是**开源的、能本地跑**的。
> 模型这块取决于你用哪家、跑多少量——**这个数跟你实际用量强相关，我瞎说会误导你。**
> 真要估算，得先定清楚'每天多少条 issue'。"

---

#### ❓ "这套东西成熟吗？能上生产吗？"

> "**不能。** 它现在是 alpha。
> 我今天讲的是它**能做到什么**，不是它现在**稳到什么程度**。这两件事差得很远，我不想混着讲。"

---

#### ❓ "我们用的是 Azure DevOps，这能接吗？"

> "能，而且挂载点比一般工具多——**Work Item、Board、PR 审批、Pipeline 全是现成的接口**。
>
> 我今天故意用 CSV，是为了**不依赖内网权限**。
> 真要接，'审批'这一步甚至不用新造轮子——走 **PR 审批**或 **Work Item 状态流转**就行。"

---

### 3.2 技术追问

| 问题 | 答案要点 | 详见 |
|---|---|---|
| "具体装了哪些插件？" | 最小五件套：`agent-presets` / `tool-subagent` / `tool-workflow` / `plan-mode` / `user-approval` | `演讲稿.md` 第三部分 |
| "怎么创建一个岗位？" | 三层：agent preset 定是谁 / permission preset 定能碰什么 / 具名 subagent 工具实例定这次起谁 | `演讲稿.md` 第四部分 |
| "DSH 有 agent team 功能吗？" | **没有。** 是用原语拼出来的模式 | `03` §2.2 |
| "门到底在哪？" | **运行时拦的**，不是脚本里的 if | `演讲稿.md` §5.4 |
| "能按项目动态建团队吗？" | **岗位可以动态选，权限不能动态长** | `演讲稿.md` 第七部分 |
| "一个会话里能有多个岗位吗？" | 能，靠挂多个具名 `dsh-tool-subagent` 实例 | `演讲稿.md` §7.1③ |
| "为什么验收者要换模型？" | 同一个模型有同样的盲区 | `演讲稿.md` §4.4 |
| "OpenDesign 怎么接 DSH？" | **原生 runtime adapter**，不是 MCP：每轮 `dsh --profile open-design --stdio` | `04` §5.2 |
| "为什么 `od mcp install` 名单里没有 deepseek-harness？" | 因为 DSH 走原生通道，接得更深 | `04` §5.3 |
| "Linux 上能装 OpenDesign 吗？" | 官方无预构建，只能源码 | `04` §5.3 + §5.5 |
| "它能后台跑吗？" | **一次 workflow 调用是阻塞的**，父级等整张扇出结束 | `03` §3.3 |
| "这套岗位设计换成别的工具行吗？" | ✅ **行。这正是我想讲的** | — |

### 3.3 万能兜底

> "这个我确实没试过，我不瞎说。**回头我试完告诉你。**"

**"我不知道"是全场最安全的一句话。** 你讲的是畅想，没人要求你全知。

---

## 四、上台前最后三件事

1. **第 3 步的那 5 秒沉默，别用话把它填满。**
2. **演示一卡，3 秒内切截图，不解释、不救场。**
3. **结尾不说"我们准备落地"，只说"以上都是我一个人的推演"。**
