# 演讲稿 · AI 第一次可以有编制

> **主交付物。** 按你的八点结构组织：介绍 → 安装命令 → 用什么插件 → team member 怎么创建与职责 → 怎么做 workflow → agent 独有 skill → 能不能按项目/功能点动态建 Teams → 项目背景故事怎么写。
>
> **排版约定**：
> - 🎤 = **台上说的话**（可直接照念，口语化）
> - 📋 = 讲稿备注（不要说出口，是给你的提示）
> - 🔧 = 附录弹药（台上**不念**，被问到才用）
>
> 技术事实基线见 `README.md`；团队机制细节见 `02-三人团队设计.md`。

---

## 开场（0:00–0:02）

🎤 "过去两年，我们每个人都在用 AI 工具。

我先猜一下在座各位电脑上装了什么——Claude Code、Codex、OpenCode，大概都有。我自己这三样都装了。

**今天我不讲这些工具怎么用。** 这些你们比我会用。

我想讲一件刚刚才变得可能的事：**AI 第一次可以有编制。**

有岗位。有职责。有权限。有审批。有留痕。

我会拿一个 bug 从头演到尾：从它被提出来，到它被修好、被合并，中间谁干了什么、**谁在哪一步必须说'可以'**。"

📋 开场只干一件事：把听众脑子里的"工具"换成"编制"。**不要提架构、不要提插件名。**

---

## 第一部分 · 介绍（0:02–0:05）

### 1.1 两个东西，一句话分工

🎤 "今天会用两个开源工具。它们的分工特别清楚，一句话：

> **DeepSeek Harness 管'这件事谁来做、做到哪一步必须停'。**
> **OpenDesign 管'做出来长什么样'。**

一个管**编制**，一个管**标准**。"

### 1.2 DeepSeek Harness 是什么

🎤 "第一个叫 DeepSeek Harness，命令是 `dsh`。

它不是聊天框，是 **agent 运行时**。

什么叫运行时？它有三个入口，对应三种使用者：

| 入口 | 谁在用 |
|---|---|
| `dsh web` | **人**——我看着它干活，随时插话 |
| `dsh --profile headless "任务"` | **机器**——跑一件活、出结果、退出，CI 里调 |
| `dsh --profile acp` / `sdk` | **别的系统**——把它当引擎接进去 |

所以它的设计目标不是'更好用的对话框'，是**能被人、被机器、被别的系统三种方式驱动的执行引擎**。"

🎤 "还有一点最重要：**它是插件化的。你要的能力是'装上去'的，不是内置的。**

这正好是今天主题的基础——**编制本来就是配出来的，不是买来的。**"

📋 诚实标签：🟡 当前是 alpha。这句一定要说，见 1.5。

### 1.3 OpenDesign 是什么

🎤 "第二个叫 OpenDesign，命令是 `od`。

它是**开源的 Claude Design 替代品**，Apache-2.0，本地优先的桌面应用。

它最反直觉的一点是：**它自己不带模型，也不带 agent。**

> **它不认识模型，它认识你电脑上已经装好的 CLI。**

你 PATH 上那些 `claude`、`codex`、`cursor-agent`、`opencode`——它扫一遍就都认出来了，拿它们当设计引擎。"

### 1.4 两者的关系（这是全场技术上的第一个惊讶点）

🎤 "那这两个怎么合作？

**不是 MCP，也不是 skill，是原生 runtime adapter。**

具体讲：OpenDesign 每一轮直接执行 `dsh --profile open-design --stdio`。

跑起来之后，结构化思考、工具调用、取消（连进程树一起回收）、会话冷恢复、实时模型发现——全都支持。"

📋 幻灯片这里画一张箭头图：`OpenDesign ──原生 runtime──▶ dsh`。
⚠️ **不要画成 MCP 连线**，这是最容易说错的地方。如果有人说"那不就是 MCP 吗"，答案在第七部分附注。

### 1.5 诚实的自我定位（重要，别省）

🎤 "在往下讲之前，我得说清楚三件事。

**第一，`dsh` 现在是 alpha。** 我今天不是推荐你们上生产，是**拿它把一件事推演清楚**。

**第二，DSH 里没有一个叫'agent team'的开关。** 团队是**用它的零件拼出来的**。
——这一点对你们反而是好消息：**你们学到的是方法，不是某个产品的按钮。**

**第三，OpenDesign 接 DSH 这条路，官方只在一个比较旧的 dsh 版本上验过。** 所以涉及到这一块，我都会标出来'这是没验证的区间'。"

📋 这三句说完，你在 IT 同事心里的可信度会直接上一个台阶。**主动交代边界，比讲得漂亮管用。**

---

## 第二部分 · 安装命令（0:05–0:07）

📋 这一段**台上只讲 3 分钟**，但命令要写全。听众里真会去装的可能只有两三个人——**为这两三个人写清楚，是"可复制"的兑现**。

### 2.1 装 DeepSeek Harness

🎤 "装它一句话：

```bash
npm install -g @deepseek-ai/dsh
dsh --version      # 确认装上了
dsh web            # 打开它的 Web 界面
```

前提是 **Node ≥ 24**（或者 `^22.19`）。

我刚才查了一下，npm 上现在的 latest 是 **`0.1.5-rc.1`**——已经是 RC 了，比我本机这个 alpha 新。"

🔧 **附录**
| 项 | 值 |
|---|---|
| 包名 | `@deepseek-ai/dsh`（公开包） |
| 全局安装位置 | `npm prefix -g`（本机是 `C:\Users\xinjiboy\AppData\Roaming\npm`） |
| npm latest | `0.1.5-rc.1` |
| 本机核验版本 | `0.1.5-alpha.2` |
| Node 要求 | `^22.19` 或 `>=24`（本机 v24.19.0） |
| 其他入口 | `dsh --profile headless "任务"`、`dsh --profile acp`、`dsh --profile sdk`、`dsh plugin --profile <名字> <pnpm 参数>` |
| 检查配置 | `dsh --dump-config`（不启动就能看合成后的配置树） |

### 2.2 装 OpenDesign

🎤 "OpenDesign 有三条路，按成本从低到高：

**第一条，桌面应用（推荐）。**
- macOS：`brew install --cask open-design`（要求 macOS ≥ 12）
- Windows：下载 x64 安装包

**第二条，接进你已有的 CLI。**
```bash
od mcp install claude     # 或者 codex / cursor / opencode ...
```
支持 17 个 agent。`--print` 可以先干跑看一眼，`--uninstall` 卸载。

**第三条，Docker。**
```bash
git clone https://github.com/nexu-io/open-design.git
cd open-design/deploy
cp .env.example .env
echo "OD_API_TOKEN=$(openssl rand -hex 32)" >> .env
docker compose up -d
# 打开 http://127.0.0.1:7456
```
用户名 `open-design`，密码就是那个 token。"

🎤 "**这里有两个坑，我踩过，先说给你们省点时间：**

**第一，Linux 没有官方预构建产物。** 想在 Linux 服务器上跑，只能源码构建。

**第二，`od` 这个命令没有 npm 包。** 你 `npm i -g od` 装不到东西——它**随桌面应用附带**。macOS 上还会撞到 `/usr/bin/od` 的遮蔽问题。"

🔧 **附录 · 安装事实**
| 项 | 值 |
|---|---|
| macOS | `brew install --cask open-design`（cask 当前 0.22.2，macOS ≥ 12） |
| Windows | x64 安装包 |
| Linux | ❌ 无官方预构建，仅源码（issue #4368） |
| `od mcp install` 名单 | claude · claude-desktop · codex · reasonix · raven · cursor · copilot · openclaw · antigravity · pi · vibe · hermes · cline · kimi · kiro · trae · opencode |
| hosted 写法 | `curl -fsSL https://open-design.ai/install.sh \| sh -s <agent>` |
| Docker 端口 | `7456` |
| `od` 的 npm 包 | **没有。** registry 上的 `open-design` 是 2021 年的历史占位包，不是本项目 |

### 2.3 把两个接通

🎤 "最后一步，让 OpenDesign 把 DSH 当引擎：

```bash
od agent setup deepseek-harness
```

它在图形界面里的等价路径是：**设置 → 模型与提供商 → 本机 CLI → 重新扫描 → 选中 deepseek-harness → 安装连接组件 → 测试**。

**它做什么、不做什么，一定说清楚：**

| ✅ 会做 | ❌ 不会做 |
|---|---|
| 把 OpenDesign 内嵌、**做过哈希校验**的 profile 组件装进你 dsh 的 `open-design` profile | **不安装 dsh** |
| 装完重新扫描 + 连接测试 | **不升级 dsh** |

所以顺序是**先装 dsh，再装 OpenDesign，最后跑这条命令**。"

🎤 "还有一个前置条件特别容易忘：**OpenDesign 的后台服务必须在跑。**
没跑的话这条命令会直接以 **exit 64** 退出，报 `daemon-not-running`。

另外，`od agent` 只有 `setup deepseek-harness` 这一个用法，**没有 `list`、没有 `remove`、也没有 `doctor`**——写别的它会打帮助然后 exit 2。"

### 2.4 装完自查清单

🎤 "装完请务必自查这五条，不用等演示翻车才发现：

```bash
node --version                      # 要 >= 24 或 ^22.19
dsh --version                       # 能打印版本
dsh web                             # 能起来，并且 Key 配好了
od --help                           # od 命令存在（随桌面应用装的）
od agent setup deepseek-harness     # daemon 在跑，才不报 exit 64
```

📋 最后一条是**唯一需要 daemon 在跑**的命令——演示前一定先确认，否则现场 exit 64 很难看。

---

## 第三部分 · 用什么插件（0:07–0:09）

🎤 "刚说 DSH 是插件化的，那具体装什么？我按**流程环节**给你们排一遍——这样你们回去对着自己的流程就能挑。"

### 3.1 按环节排的插件表

| 环节 | 插件 | 状态 |
|---|---|---|
| **接单**（webhook 进来） | `dsh-webhook`、`dsh-webhook-github` | 🟡 可选 overlay，默认不含 |
| **岗位定义** | `dsh-agent-presets` | 🟢 |
| **权限分级** | `dsh-permission-presets`、`dsh-sandbox-policy`、`dsh-fs-sandbox` | 🟢 |
| **岗位实例化 / 委派** | `dsh-tool-subagent`、`dsh-subagent-spawn-in-process`、`dsh-subagent-fork-in-process` | 🟢 |
| **编排** | `dsh-tool-workflow`、`dsh-workflow`、`dsh-workflow-worker-thread` | 🟢 |
| **先规划再执行** | `dsh-plan-mode` | 🟢 |
| **审批门** | `dsh-user-approval` | 🟢 |
| **技能** | `dsh-skill`、`dsh-skill-filesystem`、`dsh-tool-skill` | 🟢 |
| **留痕 / 恢复** | `dsh-session-persistence-jsonl`、`dsh-session-checkpoint-policy`、`dsh-session-query` | 🟢 |
| **长跑目标** | `dsh-goal`、`dsh-goal-round-driver`、`dsh-tool-goal` | 🟢 |
| **全新上下文迭代** | `dsh-tool-ralph` | 🟢 |
| **后台任务** | `dsh-jobs-local`、`dsh-tool-jobs` | 🟢 |
| **接外部工具** | `dsh-mcp-client` | 🟢 |
| **定时触发** | `dsh-schedule` | 🟡 可选 overlay |
| **成本可见** | `dsh-token-meter` | 🟢 |

> 🟢 = 在本文核验的 `0.1.5-alpha.2` 上默认可用（但 DSH 本体仍是 🟡 alpha）
> 🟡 = 可选 overlay / alpha，默认 profile 不含，要自己接

### 3.2 最小可用集合（只有五个）

🎤 "如果你们只想先跑通一条最小链路，**只需要这五个**：

1. **`dsh-agent-presets`** —— 定义岗位
2. **`dsh-tool-subagent`** —— 把岗位实例化出来
3. **`dsh-tool-workflow`** —— 把岗位串成流程
4. **`dsh-plan-mode`** —— 让它在动手前停下来出计划
5. **`dsh-user-approval`** —— 那道必须有人点头的门

其余都是锦上添花。

> **注意：接单用的 `webhook` 不在这五个里面。** 因为它默认没装。所以'团队怎么组'和'单子怎么自动进来'是两件事——**我先讲团队，单子怎么进来最后讲。**"

📋 这一段的作用是防被问："我照你说的装了，怎么没有自动接单？"——**主动切开这两个概念**。

---

## 第四部分 · team member 怎么创建 + 职责说明（0:09–0:15）★核心

### 4.1 先纠正一个直觉

🎤 "先说一个你们大概会猜错的地方。

你们可能以为，创建 team member 就是'新建几个 agent'。

**不是。** 在 DSH 里，一个岗位是由**三层**叠出来的：

| 层 | 机制 | 它决定什么 |
|---|---|---|
| **① 岗位定义** | **agent preset** | 这个岗位**是谁**、会什么、带哪些**专属技能** |
| **② 权限** | **permission preset** | 它**能碰什么**、要不要人批 |
| **③ 实例化** | **具名 subagent 工具实例** | **这一次**起谁、用什么人格、用哪个模型 |

**为什么这么设计？** 因为把'它是谁'和'它能干什么'拆开，权限才管得住。"

### 4.2 第一层：agent preset = 岗位说明书

🎤 "preset 就是一个目录，核心是一个 `agent.cordis.yml`，里面写三样东西：

> **这个岗位的工具 + 提示词段落 + 它自己的技能目录。**

放在哪？两个地方：
- 你自己的：`<dshHome>/.agent-presets`（就是 `~/.dsh/.agent-presets`）
- 或者你在配置里加一个扫描根目录：

```yaml
- name: '@deepseek-ai/dsh-agent-presets'
  config:
    default: standard
    roots:
      - path: ~/company-presets
        trust: system
```

🎤 "**创建新岗位的方式很朴素：复制一个现有的，然后改。**

复制会把整个目录搬过来——组装、元数据、**技能目录**、资产。id 必须长这样：小写字母数字加连字符。

还有一条要记住：**只有空会话才能切换 preset。** 会话跑起来了就换不了岗。"

📋 ⚠️ **这里有个必须交代的限制，否则第七部分讲不通**：
"**子 agent 会加入它父方的组装**"——也就是说，**你不能靠 preset 给单个 subagent 换岗**。那一个会话里怎么有多个岗位？答案在 4.4。

### 4.3 第二层：permission preset = 权限

🎤 "第二个东西把**沙箱模式**和**审批策略**捆成一个名字：

```yaml
- name: '@deepseek-ai/dsh-permission-presets'
  config:
    presets:
      read-only:
        sandbox: read-only
        approval: ask
      workspace-write:
        sandbox: workspace-write
        approval: ask
      danger-full-access:
        sandbox: danger-full-access
        approval: never
    defaultPreset: workspace-write
```

🎤 "内置的就两档：`workspace-write` 和 `danger-full-access`。
用 `/permission` 命令切换，不带参数就报告当前是哪一档。

**关键是：创建会话那一刻决定，之后改动不影响已经在跑的会话。**"

### 4.4 第三层：具名 subagent 工具实例 = 真正的"创建成员"★

🎤 "**这一层才是你们要找的'怎么创建 team member'。**

`dsh-tool-subagent` 这个插件，**每挂一个实例就多一个工具**。而且实例的工具名必须不一样。

所以在配置里，你可以直白地这样写：

```yaml
# 分析师：只读，专属技能
- name: '@deepseek-ai/dsh-tool-subagent'
  config:
    provider: spawn
    toolName: analyst
    persona: |
      你是分析师。你只做三件事：评估、定位、出计划。
      你绝对不能修改任何文件。你的产出必须是一份结构化计划。
    toolFilter: [read_file, search, list_files]
    maxDepth: 0

# 实现者：能写工作区，但推不出去
- name: '@deepseek-ai/dsh-tool-subagent'
  config:
    provider: spawn
    toolName: builder
    persona: |
      你是实现者。你只能执行一份"已被批准"的计划。
      拿到计划前不得动手。你可以改工作区文件，但不得提交、不得推送。
    toolFilter: [read_file, search, write_file, run_tests]
    maxDepth: 0

# 验收者：独立上下文，换一个模型来审
- name: '@deepseek-ai/dsh-tool-subagent'
  config:
    provider: spawn
    toolName: verifier
    persona: |
      你是验收者。你看的是代码，不是实现者的说辞。
      你没有看过实现过程的任何记录。请独立复查并给出证据。
    toolFilter: [read_file, run_tests]
    agentOptions:
      model: <另一个模型>
    maxDepth: 0
```

🎤 "**看这三段的区别——这就是岗位说明书的写法：**

| 配置项 | 它是什么 |
|---|---|
| `toolName` | **岗位名**。一挂就是三个工具：`analyst`、`builder`、`verifier` |
| `persona` | **职责说明**。每个子 agent 独立的人格与边界 |
| `toolFilter` | **权限裁剪**。分析师的工具列表里根本没有写文件 |
| `agentOptions` | **给这个岗位单独指定模型** |
| `maxDepth: 0` | 禁止它再往下委派——**防止无限套娃** |

🎤 "最后那个 `agentOptions` 我想特别点一下。

**我给验收者配了一个不同的模型。**

为什么？因为如果实现者和验收者是同一个模型，它们会有**同样的盲区**——同一个模型看不出来的问题，换它自己再审十遍也看不出来。

**换一个模型来审，才是真的独立。** 这在人和人之间也是这样。"

### 4.5 三个岗位的职责说明（可直接抄）

🎤 "把它写成岗位说明书，就是这三张卡：

**① 分析师**
> **目标**：把一个模糊的 bug 变成一份可执行的计划。
> **产出**：`{ 类型, 严重度, 影响面, 根因假设, 证据[], 不确定项[], 实施计划, 验证方式 }`
> **禁止**：修改任何文件。
> **停止条件**：**产出计划后必须停**，等人批准。
> **专属技能**：故障复现、日志分析、影响面评估。

**② 实现者**
> **目标**：执行一份**已被批准**的计划。
> **产出**：`{ 变更文件[], 自测结果 }`
> **禁止**：提交、推送、改动计划之外的任何文件。
> **停止条件**：干完停。
> **专属技能**：代码修改、单元测试。

**③ 验收者**
> **目标**：独立判断这份改动到底行不行。
> **产出**：`{ 结论, 发现的问题[], 测试证据 }`
> **禁止**：看实现过程的记录；修任何东西。
> **停止条件**：出结论。
> **专属技能**：代码审查、测试执行。

🎤 "注意每一张卡都有一栏叫**'禁止'**。

**岗位说明书里最值钱的不是'你该做什么'，是'你不该做什么'。**"

📋 如果被问"为什么不设分诊员 / 为什么审查和验证合成一个人"，答案在 `02-三人团队设计.md` 的「为什么只有三个人」。

---

## 第五部分 · 怎么做 workflow（0:15–0:18）

### 5.1 workflow 是什么

🎤 "岗位有了，怎么把它们串起来？用 `workflow`。

它是一个**给模型用的工具**：模型写一段 **JavaScript 编排脚本**，脚本把活扇出给各个岗位，最后返回一个 JSON。

三个要点：

1. **脚本是代码，不是配置。** 你能写循环、写条件。
2. **脚本里没有文件系统、没有网络、没有定时器。**
   → **脚本只负责协调，干活的是 agent。**
3. **父级只看到最终结果**，看不到中间的消息。子 agent 的活不会污染父级上下文。"

🔧 **附录 · 一次 workflow 调用是阻塞的**
父级轮次会**一直等到整张扇出结束**才继续——**没有"后台启动再轮询"这种用法**。子 agent 中间过程对模型不可见。
（这条被问到时答，也是演示时"它安静地跑、然后一口气出结果"的解释。）

### 5.2 五个钩子

| 钩子 | 作用 | 关键细节 |
|---|---|---|
| `agent(prompt, opts)` | 起一个 agent 跑到完成 | 带 `schema` → 返回**校验过的对象**；失败 → `null` |
| `phase(title)` | 进度分段 | 展示用 |
| `pipeline(items, ...stages)` | 每个 item 独立走完各阶段，**无栅栏** | 某项抛错 → 该项变 `null`，跳过剩余阶段 |
| `parallel(thunks)` | 并发跑，**等全部完成**（有栅栏） | 抛错的 → `null` |
| `log(msg)` | 打日志 | — |

🎤 "`agent` 的选项里能指定 `label`、`phase`，还有 `provider` 和 `model`——**这就是给单个岗位换模型的地方**。

顺带说一句：`agentType` / `isolation` / `effort` 这几个参数是**不接受的**，传了会直接报错。"

### 5.3 编排脚本长什么样

```js
// ── 阶段 ①：分析师 ────────────────────────────────
phase("① 分析师：评估与计划");

const triage = await agent(TRIAGE_PROMPT, {
  label: "分析师/评估",
  schema: {
    type: "object",
    properties: {
      type:      { type: "string", enum: ["bug", "feature", "duplicate"] },
      severity:  { type: "string", enum: ["P0", "P1", "P2", "P3"] },
      impact:    { type: "string" },
      uncertain: { type: "array", items: { type: "string" } },  // 必须承认不确定项
    },
    required: ["type", "severity", "impact", "uncertain"],
    additionalProperties: false,
  },
});

const plan = await agent(PLAN_PROMPT, {
  label: "分析师/计划",
  schema: {
    type: "object",
    properties: {
      files:  { type: "array", items: { type: "string" } },
      steps:  { type: "array", items: { type: "string" } },
      verify: { type: "string" },
    },
    required: ["files", "steps", "verify"],
    additionalProperties: false,
  },
});

// ── ⭐ 硬门 ──────────────────────────────────────
// ⚠️ 这三行是【形式】，不是强制点。真正的拒绝在运行时（见 5.4）。
if (args.approved !== true) {
  return { stage: "awaiting-approval", triage, plan };
}

// ── 阶段 ②：实现者 ──────────────────────────────
phase("② 实现者：按已批准的计划开发");
const impl = await agent(IMPL_PROMPT + JSON.stringify(plan), { label: "实现者" });

// ── 阶段 ③：验收者（两个方向并行）──────────────
phase("③ 验收者：复查与验证");
const [review, verify] = await parallel([
  () => agent(REVIEW_PROMPT),   // 读 diff，不看实现者的推理
  () => agent(VERIFY_PROMPT),   // 跑测试
]);

return { triage, plan, impl, review, verify };
```

🎤 "脚本不复杂。我想让你们注意三件事：

**第一，每个 `agent()` 都带 `schema`。** 意思是**它必须吐结构化数据，不能吐一段散文**。不然后面没法接。

**第二，分析师必须填 `uncertain`——不确定项。**
> **一个不承认自己不确定的分析师，比一个说'我不确定'的分析师危险得多。**

**第三，第三阶段用了 `parallel`。** 复查和跑测试互不依赖，所以并行。

`pipeline` 和 `parallel` 怎么选？——**`pipeline` 无栅栏，一项慢不拖累别人；`parallel` 有栅栏，会被最慢的拖住。所以默认用 `pipeline`，`parallel` 是例外。**
（这句是我的经验建议，不是官方口径。）"

### 5.4 ⭐ 门到底在哪（这一段最容易被讲错）

🎤 "现在讲今天最关键的一个技术判断。

脚本里有那么一句 `if (args.approved !== true)`。**你们可能以为门就是那个 if。**

**不是。**

> **门是运行时拦的，不是脚本自觉遵守的。**

那个 if 只是**把批准结果带进编排**的形式。真正的拒绝发生在**审批 seam**——它在**工具调用层**拦截。

**这意味着两件事：**
- 只要把'启动实现者'这一步纳入审批 seam 的管辖，**脚本里写不写那个 if 都拦得住**；
- 反过来，**如果没纳入管辖，脚本里写十个 if 也没用。**

我为什么强调这个？因为它回答了一个根本问题：

> **流程的可靠性，不能建立在 agent 的自觉上。**

它该建立在**它绕不过去的机制**上。这就是为什么权限要单独做成一层（第四部分第二层），而不是写在提示词里。"

📋 这一段是全场的**技术最高点**。讲慢一点，讲完停一下。

---

## 第五部分附 · ★ 现场演示（0:18–0:22，4 分钟）

🎤 "讲了这么多，我跑一遍给你们看。"

📋 演示的完整步骤、话术、截图兜底方案全在 **`05-演示与答疑.md`**。这里只说三件事：

**① 演示只证明三件事**
1. 它**只读地**分析并给出计划
2. ⭐ 它**会停下来等人**——**这一秒是全场高潮**
3. 点头之后它**真能改并验证**

**② 到第 3 步，停 5 秒别说话**

🎤 "注意——它现在停下来了。它在等我说可以。"

📋 这 5 秒很多人会忍不住继续说。**别说。** 让它安静地停在那里，让观众自己看见。

**③ 出任何问题，3 秒内切静态截图**

🎤 "我先把后面几步的样子放出来，这样不耽误大家时间。"

📋 **不说**"奇怪刚才还好好的"、**不说**"稍等我看看"、**不提**"网络"两个字。
**在台上 debug 才是丢分。** 提前截图恰恰说明你准备过。

---

## 第六部分 · agent 独有 skill（0:22–0:24）

🎤 "接着上面的问题：**不同岗位会的技能不一样，怎么做到？**

答案在第四部分已经埋了：**preset 自带 skill 目录。**

> **每个 preset 目录里，有自己的技能集合。所以分析师的技能清单和实现者的技能清单，是两个不同的列表。**

### 6.1 skill 是怎么组织的

🎤 "DSH 的 skill 机制是三层：

| 包 | 干什么 |
|---|---|
| `dsh-skill` | **统一注册表**——合并所有来源 |
| `dsh-skill-filesystem` | **本地发现**——从目录里读 skill |
| `dsh-tool-skill` | **模型访问**——让 agent 能按需加载 |

`dsh-skill` 本身**不含任何 skill 内容**，它只做合并与裁决。真正的内容来自：
- 本地目录
- **插件内嵌**（插件用 `ctx.skills.register(...)` 注册）
- 远程服务

🎤 "关键设计是**按需加载**：目录里先给 agent 看 skill 的**摘要**，它决定要用哪个，才去加载完整指令。

**这样技能多了也不会把上下文撑爆。**"

### 6.2 每个 skill 有两个开关

| 策略 | 含义 |
|---|---|
| `modelInvocable` | **模型**能不能看到并加载它 |
| `userInvocable` | **用户**能不能用命令调到它 |

🎤 "所以你可以做一个很实用的区分：

- 有些技能**只给模型用**（比如'复现步骤生成'）
- 有些技能**只给人用**（比如'一键回滚'）
- 有些两边都能用

**这一层是权限的延伸——不只是'能不能碰文件'，还有'能不能看见这个技能'。**"

### 6.3 落到你的三个岗位上

| 岗位 | 专属技能（举例） | 不该带的 |
|---|---|---|
| 分析师 | 故障复现、日志分析、影响面评估、根因假设 | 代码修改、部署 |
| 实现者 | 代码修改、单元测试、重构 | 影响面评估（会诱导它扩大范围） |
| 验收者 | 代码审查、测试执行、边界用例设计 | **代码修改**（它只能看，不能改） |

🎤 "为什么验收者**不该带**代码修改的技能？

**因为一个手上拿着锤子的人，看什么都像钉子。**

它一旦会改代码，它就会忍不住去改——那它就从'审判者'变成了'共犯'。

**技能清单本身就是一种边界设计。**"

---

## 第七部分 · 能不能按项目或功能点单独设立 / 动态创建 Teams（0:24–0:27）★

🎤 "这是我最想回答的一个问题，因为它决定了这套东西能不能**规模化**。

我先给结论，就一句：

> **岗位可以动态选，权限不能动态长。**

下面拆成两半讲。"

### 7.1 ✅ 能做的三件事

#### ① 按项目分设不同的团队（完全可行）

🎤 "`dsh-agent-presets` 的 `roots` 是**一个列表**，可以扫多个目录：

```yaml
- name: '@deepseek-ai/dsh-agent-presets'
  config:
    default: standard
    roots:
      - path: ~/teams/payment
        trust: system
      - path: ~/teams/reporting
        trust: system
      - path: ~/.dsh/.agent-presets     # 你自己的
```

目录长这样：

```
~/teams/payment/
├── analyst/      # 支付项目的分析师
├── builder/
├── verifier/
└── security-auditor/   # 支付项目才有的岗位
```

🎤 "**支付团队的编制里可以多一个'安全审计员'，报表团队就不需要。**
这就是'按项目单独设立团队'。"

#### ② 按功能点动态决定这次叫谁（完全可行）

🎤 "workflow 脚本**是代码**。所以你可以这样写：

```js
// 涉及支付的改动，才叫安全审计员
const needsSecurity = triage.impact.includes("payment");

const reviewers = [() => agent(REVIEW_PROMPT)];
if (needsSecurity) {
  reviewers.push(() => agent(SECURITY_PROMPT));
}

const results = await parallel(reviewers);
```

🎤 "**这就是'动态创建 Teams'**——注意关键词：**动态的是'叫谁'，不是'它有什么权限'。**
安全审计员的权限是你事先写好的。"

#### ③ 一个会话里同时有多个岗位（可行，但要靠对机制）

🎤 "这一条有个坑，我得说清楚。

**子 agent 会加入它父方的组装**——意思是，**你不能靠 preset 给一个 subagent 单独换岗**。

那第四部分那三个具名工具实例（`analyst` / `builder` / `verifier`）是怎么来的？

> **靠挂载多个 `dsh-tool-subagent` 实例，每个实例有自己的 `toolName`、`persona`、`toolFilter`、`agentOptions`。**

**所以'一个会话里多个岗位'是配置出来的，不是运行时发明出来的。**"

### 7.2 ❌ 不能做的一件事（而且不应该做）

🎤 "那**动态创建岗位并当场给它发权限**——行不行？

**不行。而且这是设计目标，不是缺陷。**

三个理由：

**第一，preset 是受信任配置。** 官方文档原话是：
> 请将每个自行编写的 preset 视为**受信任配置**，因为它会**授予其所选插件的能力**。

一个能自己长权限的岗位，就是一个能自己给自己发钥匙的员工。

**第二，权限必须预先定义，否则 fail-closed 就失效了。**
我们前面讲的门——'没有应答者就拒绝'——它成立的前提是**权限边界是事先画好的**。如果边界能运行时生成，那门拦的是什么？

**第三，会话跑起来就换不了岗。** 只有**空会话**才能切换 preset。"

### 7.3 这一段的收尾

🎤 "所以准确的说法是：

| 问题 | 答案 |
|---|---|
| 能按项目分设团队吗？ | ✅ 能，靠 preset 的多个扫描根目录 |
| 能按功能点动态决定叫谁吗？ | ✅ 能，workflow 脚本是代码 |
| 能动态创建岗位并当场发权限吗？ | ❌ **不能，也不该能** |
| 一个会话里能有多个岗位吗？ | ✅ 能，靠挂多个具名 subagent 工具实例 |

**一句话总结：**

> **编制可以按需伸缩，权限必须事先画好。**
>
> **你让 AI 自由的地方，应该是'叫谁来干活'；你绝不能让它自由的，是'它能碰什么'。**"

📋 这一段是**全场第二个最高点**（第一个是 5.4）。现场如果有人追问"那不是不够灵活吗"，回一句：
"**你要的灵活是编制灵活，不是权限灵活。这两件事混在一起，就是事故的起点。**"

🔧 **附录 · 为什么 DSH 接 OD 不是 MCP**
如果被问"别人都用 MCP，为什么这里不用"：
- OpenDesign 对 DSH 走的是**原生 runtime adapter**（runtime id `deepseek-harness`，流格式 `dsh-profile-jsonl`，每轮 `dsh --profile open-design --stdio`）
- 所以 `od mcp install` 那 17 个 agent 名单里**没有 deepseek-harness**——不是遗漏，是它接得更深
- 反过来（把 OD 当 MCP server 接回 DSH）**官方还没打通**，OD 的 phase one 明确写着 `MCP: not forwarded in phase one`。两边零件都在，但官方没给连法。

---

## 第八部分 · 项目背景故事怎么写（0:27–0:28）

🎤 "最后讲一个软技能，但它可能是今天最实用的部分。

**你们回去要跟别人讲这套东西，最大的障碍不是技术，是'凭什么'。**

一个技术演示，如果没有故事，听众只会想'so what'。

**我给你们一个五句话的模板。**"

### 8.1 五句话模板

🎤 "**第一句：一个具体的人，一个具体的麻烦。**
不要说'随着业务发展，需求日益复杂'——那是废话。
说：**'财务部的李伟，每个月 1 号要花两个小时手动对齐报表。'**

**第二句：这个麻烦为什么一直没被解决？**
通常答案是：**它不够重要，不值得排期；但又足够烦，每个月都疼。**

**第三句：传统解法要几步，卡在哪一步。**
'现在要走：提工单 → 我复现 → 我评估 → 我改 → 找人验。五步。
**卡在第一步到第二步之间——工单躺在那里等我看，平均一天。**'

**第四句：指出卡住的那一步，其实不需要人的创造力。**
> **'那一步不需要我聪明，只需要我有空。'**

**第五句：把新问题抛出来。**
> **'如果那一步能自动呢？'**

🎤 "说完这五句，你才放技术。**顺序不能反。**"

### 8.2 拿今天的例子走一遍

🎤 "今天这个例子的背景故事是这样的：

> **一、** 财务的李伟，每个月导出报表时，CSV 列名都是乱码，下游模板全废。
>
> **二、** 这个 bug 报了三周了，P2，一直排不上——因为**它不致命，但每个月都疼一次**。
>
> **三、** 现在走五步：李伟提单 → 我复现 → 我评估 → 我改 → 找人验。**卡在第一步到第二步，平均躺一天。**
>
> **四、** 但'读完这个工单、决定要不要做、打算怎么改'——**这一步不需要我聪明，只需要我有空。**
>
> **五、** 那如果这一步，交给一个有编制的团队呢？"

🎤 "注意这个故事里**没有任何技术名词**。它只有：一个具体的人、一个具体的疼、一句'这一步不需要我聪明'。

**技术是后面才出场的，而且只是配角。**"

### 8.3 三个写作纪律

| ❌ 别写 | ✅ 改写成 |
|---|---|
| "随着业务发展" | 一个具体的人 + 一个具体的动作 + 一个具体的时间 |
| "提升效率 XX%" | 描述那个疼，不要给数字（**畅想不该有 KPI**） |
| "AI 可以自动完成流程" | "那一步不需要我聪明，只需要我有空" |

🎤 "最后一条最重要：**你的故事主角是人，不是 AI。**

人有一个麻烦，AI 只是后来出场的工具。
**如果故事主角是 AI，听众会警惕；主角是人，听众会代入。**"

---

## 收尾（0:28–0:30）

### 9.1 三个可以带走的判断

🎤 "**第一，门槛不在模型多聪明，在你把权限切得多细。**
同一个模型，权限给错就是灾难，给对了就是同事。

**第二，让 AI 停下来 的地方，比让它跑起来的地方更值钱。**
今天我花了最多时间讲的，不是它写得多快，是**它在第五部分那一步停住了**。

**第三，最该先自动化的，不是'写代码'，是'读完 issue 然后决定要不要做'。**
那件事我们每天花最多时间，而且最不需要人的创造力。"

### 9.2 最小可试动作

🎤 "回去你们可以试一件**最小**的事，别的都不用改：

> **下次收到一个 bug，先别自己动手，让 agent 先给你一份'我打算这么改'的计划。**

就这一步。

你会发现，**光是'它先出计划、我点头'这一个动作，就已经改变你和它的关系了**。"

### 9.3 不讲的话

📋 结尾**绝对不要**说：
- ❌ "我们部门接下来准备落地"
- ❌ "预计能节省 XX%"
- ❌ "AI 以后会取代……"

**这是畅想，不是立项。** 说一句：

🎤 "以上都是我一个人的推演，没有立项、没有试点、也没有时间表。
**如果哪一条你觉得可行，那是你自己的判断，不是我的承诺。**"

---

## 附录 A · 一页速查

### 八句话术卡（背这八句就够）

| # | 位置 | 话 |
|---|---|---|
| 1 | 开场 | 不是更强的工具，是**有编制的团队** |
| 2 | ① | DSH 不是聊天框，是 agent 运行时；**你要的能力是装上去的** |
| 3 | ① | 装完之后，**它不认识模型，它认识你电脑上已经装好的 CLI** |
| 4 | ④ | 创建岗位靠**三层**：preset 定是谁、permission 定能碰什么、具名工具实例定这次起谁 |
| 5 | ④ | **验收者用另一个模型**——同一个模型看不出自己的盲区 |
| 6 | ⑤ | **门是运行时拦的，不是脚本自觉遵守的** |
| 7 | ⑦ | **岗位可以动态选，权限不能动态长** |
| 8 | ⑧ | 那一步**不需要我聪明，只需要我有空** |

### 三条红线

| ❌ 不说 | ✅ 改说 |
|---|---|
| "我们准备落地" | "我只是**推演**一遍" |
| "这东西很简单" | "门槛不在技术，在**权限怎么切**" |
| "AI 以后会取代……" | 打住，**不接这个句式** |

### Q&A 快速索引

| 问题 | 去哪 |
|---|---|
| 具体用了哪些插件？ | 第三部分 3.1 |
| 怎么创建岗位？ | 第四部分 4.4 |
| 门到底在哪？ | 第五部分 5.4 |
| 能动态建团队吗？ | **第七部分** |
| 为什么不用 MCP？ | 第七部分 🔧 附注 |
| 会裁员吗 / 安全吗 / 多少钱 | `05-演示与答疑.md` |
| 团队为什么只有三个人 | `02-三人团队设计.md` 「为什么只有三个人」 |

---

## 附录 B · 时间分配与砍单

### B.1 三档预案

| 部分 | 完整版 | 标准版 | 保命版 |
|---|:---:|:---:|:---:|
| 开场 | 2 | 2 | 2 |
| ① 介绍 | 3 | 2 | — |
| ② 安装命令 | 2 | 1 | — |
| ③ 用什么插件 | 2 | 1 | — |
| **④ team member ★** | **6** | **5** | **6** |
| ⑤ 怎么做 workflow | 3 | 2 | — |
| **★ 现场演示** | **4** | **4** | **4** |
| ⑥ agent 独有 skill | 2 | 1 | — |
| **⑦ 动态 Teams ★** | **3** | **2** | **2** |
| ⑧ 项目背景故事 | 1 | 1 | — |
| 收尾 | 2 | 1 | 2 |
| **合计** | **30** | **22** | **16** |

> 📌 **各章节标题里的时间码 = 「完整版」这一列。** 换用标准版或保命版时，按本表自行调整节奏。

> ⚠️ **注意：8 个点全讲 + 现场演示，30 分钟是紧的。**
> 完整版能成立的前提是 ① ② ③ 讲得快——**这三段的细节全部在文档里，台上点到为止**。

### B.2 砍单决策表

| 超时了，砍什么 | 为什么可以砍 | 损失 |
|---|---|---|
| ② 安装命令的逐条讲解 | 命令全写进文档了，真会装的人自己看 | 无 |
| ③ 插件全表 | 只讲"最小五件套"就够 | 无 |
| ① 的介绍展开 | 三句话能说完 | 少一点背景 |
| ⑥ 的岗位技能表 | 说一句"每个 preset 自带技能目录"就够 | 少一个细节 |
| ⑤ 的脚本细节 | 只留"脚本只协调"+"门在运行时"两句 | 少一点可信度 |
| **④ team member 三层机制** | ❌ **不准砍** | "怎么建"没了 |
| **⑦ 动态 Teams** | ❌ **不准砍** | "能建多大"没了 |
| **演示里"它停下了"那一步** | ❌ **不准砍** | 全场最值钱的一秒 |
| **⑧ 五句话背景故事模板** | ❌ 尽量别砍 | 这是最实用的部分 |
| **收尾三个判断** | ❌ **不准砍** | 目标兑现点 |

> **决策原则：砍支撑，不砍命根子。**
> 命根子是 ④ ⑦ ⑧ + 演示 + 收尾——**"怎么建"、"能建多大"、"为什么值得建"**。

### B.3 幻灯清单（14 页，宁少勿多）

| # | 页面 | 部分 |
|---|---|---|
| 1 | 一行字：**AI 第一次可以有编制** | 开场 |
| 2 | DSH 三种入口表 | ① |
| 3 | `OpenDesign ──原生 runtime──▶ dsh` 🟡 | ① |
| 4 | 安装命令（三行） | ② |
| 5 | 最小五件套插件 | ③ |
| 6 | **创建岗位的三层机制** | ④ ★ |
| 7 | **三个岗位的职责卡**（三张，含"禁止"栏） | ④ ★ |
| 8 | 具名 subagent 实例配置（三段 YAML 对比） | ④ |
| 9 | workflow 脚本骨架 | ⑤ |
| 10 | ⭐ **门是运行时拦的，不是脚本自觉遵守的** | ⑤ |
| 11 | skill 的两个开关（`modelInvocable` / `userInvocable`） | ⑥ |
| 12 | **岗位可以动态选，权限不能动态长** | ⑦ ★ |
| 13 | 支付团队多一个"安全审计员"（目录树） | ⑦ |
| 14 | 五句话背景故事模板 → 三个判断 | ⑧ + 收尾 |

> **第 10 页和第 12 页是整场的记忆点。** 建议这两页做成**大字号、无配图**，全场只留一句话在上面。
